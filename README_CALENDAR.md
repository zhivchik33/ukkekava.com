# Інструкція для адміністратора: Управління подіями в календарі

## 📅 Як додавати нові події

### 1. Відкрийте файл `data/events.json`

Цей файл містить всі події календаря у форматі JSON.

### 2. Структура події

Кожна подія має наступну структуру:

```json
{
  "title": "Назва події",
  "time": "18:00",
  "location": "Місце проведення",
  "description": "Детальний опис події"
}
```

### 3. Приклад додавання нової події

```json
{
  "events": {
    "2025-03-15": [
      {
        "title": "Український фестиваль весни",
        "time": "15:00",
        "location": "Центральний парк Кекаві",
        "description": "Святкування приходу весни з традиційними українськими обрядами та забавами."
      }
    ]
  }
}
```

## 📝 Правила форматування

### Дата
- Формат: `YYYY-MM-DD` (наприклад: `2025-03-15`)
- Використовуйте двозначні числа для місяця та дня

### Час
- Формат: `HH:MM` (наприклад: `18:00`, `09:30`)
- Використовуйте 24-годинний формат

### Назва події
- Коротка та зрозуміла
- Українською мовою
- Без спеціальних символів у назві файлу

### Місце проведення
- Точна адреса або назва закладу
- Якщо місце ще не визначено, вкажіть "Місце буде уточнено"

### Опис
- Детальна інформація про подію
- Що буде відбуватися
- Для кого призначена подія
- Що потрібно взяти з собою (якщо потрібно)

## 🔧 Технічні деталі

### Файли, що відповідають за календар:
- `js/calendar.js` - логіка календаря
- `data/events.json` - дані подій
- `css/style.css` - стилі календаря
- `index.html` - HTML структура

### Як працює система:
1. Календар завантажує події з `data/events.json`
2. Користувачі можуть переглядати події
3. Користувачі можуть додавати події до свого календаря (.ics файл)
4. Тільки адміністратор може редагувати події через код

## ⚠️ Важливі зауваження

1. **Не видаляйте** існуючі події без потреби
2. **Зберігайте** правильний JSON формат
3. **Перевіряйте** синтаксис JSON після редагування
4. **Тестуйте** календар після внесення змін

## 🚀 Як тестувати зміни

1. Відредагуйте файл `data/events.json`
2. Збережіть файл
3. Оновіть сторінку в браузері
4. Перевірте, чи з'явилася нова подія в календарі

## 📞 Підтримка

Якщо виникли проблеми з календарем:
1. Перевірте синтаксис JSON
2. Перевірте консоль браузера на помилки
3. Переконайтеся, що файл `data/events.json` доступний

---

**Примітка**: Цей календар призначений тільки для перегляду подій. Користувачі не можуть створювати, редагувати або видаляти події - це може робити тільки адміністратор через редагування коду. 