# Гайд по оновленню сайту після деплойменту

## ✅ Коротка відповідь

**Так, це дуже просто!** Процес оновлення сайту після деплойменту **нічим не відрізняється** від звичайних комітів на GitHub. Ви просто:

1. Робите зміни локально
2. Комітите зміни (`git commit`)
3. Пушите на GitHub (`git push`)
4. **Сайт автоматично оновлюється!**

## 🔄 Як працює автоматичне оновлення

### З Vercel (рекомендовано):

1. **Перший раз:** Ви підключили GitHub репозиторій до Vercel
2. **Після цього:** Кожен `git push` до main гілки автоматично:
   - Тригерить новий деплоймент
   - Vercel бере останній код з GitHub
   - Будують та деплоять нову версію
   - Оновлюють ваш сайт (зазвичай за 1-2 хвилини)

**Це означає:** Ви працюєте як завжди, а сайт оновлюється автоматично!

## 📸 Приклад: Додавання фотографій за 2026 рік

### Крок 1: Додайте фотографії локально

```bash
# Додайте нові фотографії в папку
images/events/culture_2026_1.jpeg
images/events/culture_2026_2.jpeg
images/events/social_2026_1.jpeg
# і т.д.
```

### Крок 2: Оновіть `js/gallery.js`

Додайте нові фотографії до відповідних категорій:

```javascript
// У файлі js/gallery.js знайдіть секцію з фотографіями
const photos = {
    culture: {
        2022: [...],
        2023: [...],
        2024: [...],
        2025: [...],
        2026: [  // ← Додайте нову секцію
            { src: 'images/events/culture_2026_1.jpeg' },
            { src: 'images/events/culture_2026_2.jpeg' },
            { src: 'images/events/culture_2026_3.jpeg' },
        ],
    },
    social: {
        2022: [...],
        2023: [...],
        2024: [...],
        2025: [...],
        2026: [  // ← Додайте нову секцію
            { src: 'images/events/social_2026_1.jpeg' },
            { src: 'images/events/social_2026_2.jpeg' },
        ],
    },
    // ... інші категорії
};
```

### Крок 3: Закомітьте та запуште зміни

```bash
# Додайте всі зміни
git add .

# Зробіть коміт з описом
git commit -m "Додано фотографії за 2026 рік"

# Запуште на GitHub
git push origin main
```

### Крок 4: Готово! 🎉

- Vercel автоматично почне деплоймент (ви побачите це в Vercel Dashboard)
- За 1-2 хвилини ваш сайт оновлюється
- Нові фотографії з'являться на сайті автоматично
- **Нічого складного!**

## 📝 Приклади інших оновлень

### Додавання нового контенту на сторінку

```bash
# 1. Відредагуйте index.html або history.html
# 2. Закомітьте зміни
git add index.html
git commit -m "Додано новий розділ про події 2026"
git push origin main
# 3. Сайт оновлюється автоматично!
```

### Оновлення тексту/перекладів

```bash
# 1. Відредагуйте текст у HTML або файлах перекладів
# 2. Закомітьте
git add .
git commit -m "Оновлено переклади та тексти"
git push origin main
# 3. Готово!
```

### Зміна дизайну/стилів

```bash
# 1. Відредагуйте css/style.css
# 2. Закомітьте
git add css/style.css
git commit -m "Оновлено стилі для мобільної версії"
git push origin main
# 3. Сайт оновлюється!
```

## 🔍 Перевірка оновлень

### У Vercel Dashboard:

1. Відкрийте ваш проект на vercel.com
2. Перейдіть в "Deployments"
3. Ви побачите:
   - ✅ Успішні деплойменти (зелена галочка)
   - ⏳ Деплойменти в процесі
   - ❌ Помилки (якщо є)

### Перевірка на сайті:

1. Відкрийте ваш домен
2. Оновіть сторінку (Ctrl+F5 або Cmd+Shift+R для жорсткого оновлення)
3. Перевірте, що зміни з'явилися

## ⚠️ Важливо знати

### 1. **Кешування браузера**
   - Іноді браузер показує стару версію
   - Рішення: Жорстке оновлення (Ctrl+F5)
   - Або: Очистка кешу браузера

### 2. **Час оновлення**
   - Зазвичай: 1-2 хвилини
   - Іноді: до 5 хвилин (рідко)
   - Vercel показує статус деплойменту в реальному часі

### 3. **Помилки деплойменту**
   - Якщо щось пішло не так, Vercel покаже помилку
   - Перевірте логи в Vercel Dashboard
   - Виправте помилку та зробіть новий коміт

### 4. **Відкат до попередньої версії**
   - У Vercel можна відкотитися до попередньої версії
   - Відкрийте "Deployments" → оберіть попередній деплой → "Promote to Production"

## 🎯 Практичний приклад: Додавання 2026 року

### Повний процес:

```bash
# 1. Переконайтеся, що ви на main гілці
git checkout main
git pull origin main

# 2. Додайте фотографії в папку images/events/
# (скопіюйте файли через Finder/Explorer)

# 3. Відредагуйте js/gallery.js
# Додайте 2026 рік до кожної категорії

# 4. Перевірте локально
# Відкрийте index.html у браузері, перевірте що все працює

# 5. Закомітьте зміни
git add images/events/*_2026_*.jpeg
git add js/gallery.js
git commit -m "Додано фотографії та категорію 2026 року"

# 6. Запуште на GitHub
git push origin main

# 7. Зачекайте 1-2 хвилини
# Перевірте Vercel Dashboard - там буде показано процес деплойменту

# 8. Відкрийте ваш сайт
# Нові фотографії вже там!
```

## 💡 Поради

### 1. **Регулярні оновлення**
   - Робіть коміти часто (не накопичуйте багато змін)
   - Кожен коміт = одна логічна зміна
   - Приклад: "Додано фотографії за січень 2026" замість "Оновлено все"

### 2. **Опис комітів**
   - Пишіть зрозумілі описи
   - Приклад: "Додано фотографії за 2026 рік" ✅
   - Замість: "зміни" ❌

### 3. **Тестування локально**
   - Завжди перевіряйте зміни локально перед push
   - Відкрийте index.html у браузері
   - Перевірте, що все працює

### 4. **Backup**
   - GitHub автоматично зберігає всі версії
   - Можна відкотитися до будь-якої версії
   - Не хвилюйтеся про втрату даних

## 🔄 Порівняння: До та після деплойменту

### До деплойменту:
```bash
git add .
git commit -m "Зміни"
git push
# → Зміни тільки на GitHub
```

### Після деплойменту:
```bash
git add .
git commit -m "Зміни"
git push
# → Зміни на GitHub
# → Автоматично деплоїться на Vercel
# → Сайт оновлюється за 1-2 хвилини
```

**Різниця:** Тільки додається автоматичний деплоймент! Ви працюєте так само.

## ✅ Висновок

**Оновлення сайту після деплойменту:**
- ✅ Так само просто, як звичайні коміти
- ✅ Автоматичне оновлення (1-2 хвилини)
- ✅ Без додаткових кроків
- ✅ Всі версії зберігаються
- ✅ Можна відкотитися назад

**Це в межах розумного?** Так, це навіть простіше, ніж ви думали! Ви просто продовжуєте працювати як завжди, а сайт оновлюється автоматично.

---

**Питання?** Якщо щось не зрозуміло або виникли проблеми - звертайтеся!



